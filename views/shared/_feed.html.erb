<%= will_paginate @feed_items, class: 'pagination justify-content-center' %>

<ol class="microposts">
  <%= render @feed_items %>
</ol>

<%= will_paginate @feed_items, class: 'pagination justify-content-center' %>
